# ─────────────────────────────────────────────────────────────────────────────
# Grabbite — Python Dependencies
# ─────────────────────────────────────────────────────────────────────────────
# Install all required packages:
#   pip install -r requirements.txt
#
# For development tools (linter, tests) also run:
#   pip install -r requirements-dev.txt
# ─────────────────────────────────────────────────────────────────────────────


# ── Web Framework ─────────────────────────────────────────────────────────────
Flask==2.3.3                    # Core web framework
Flask-SQLAlchemy==3.0.5         # Database ORM (Object Relational Mapper)
Flask-Login==0.6.3              # User session & authentication management
Flask-WTF==1.2.1                # Form handling (used by admin blog forms)
Flask-Migrate==4.0.5            # Database schema migrations (Alembic wrapper)
Flask-Limiter==3.5.0            # Rate limiting on sensitive endpoints (login, signup, etc.)
Flask-Mail==0.10.0              # Transactional email (order confirmations, password reset)
Flask-SocketIO==5.3.6           # Real-time WebSocket support (live order notifications)


# ── Security ──────────────────────────────────────────────────────────────────
# Bumped to 2.3.8 to patch CVE-2023-46136 (DoS via slow multipart parsing).
# API-compatible with Flask 2.3.3 — no code changes needed.
Werkzeug==2.3.8


# ── Database ──────────────────────────────────────────────────────────────────
# SQLite ships with Python — no extra install needed for local development.
# For production, uncomment ONE of the following drivers:
#
#   PostgreSQL (recommended for production):
#     psycopg2-binary==2.9.9
#
#   MySQL / MariaDB:
#     PyMySQL==1.1.0
#
# Set DATABASE_URL in your .env to switch backends (see .env.example).


# ── Environment Configuration ─────────────────────────────────────────────────
python-dotenv==1.0.0            # Loads variables from the .env file into os.environ


# ── Image Processing ──────────────────────────────────────────────────────────
Pillow>=10.0.1                  # Resizes profile photos and uploaded restaurant/dish images


# ── Payment Gateway ───────────────────────────────────────────────────────────
# Handles online payments (UPI, card, net banking).
# The app degrades gracefully to Cash on Delivery if keys are not configured.
razorpay==1.4.1


# ── Token Signing ─────────────────────────────────────────────────────────────
# Used to generate and verify time-limited, signed password-reset URLs.
itsdangerous>=2.1.2



# ── Optional: Redis-backed Rate Limiter ──────────────────────────────────────
# By default, Flask-Limiter uses in-memory storage (single worker only).
# For multi-worker production deployments, point at a Redis instance:
#   Uncomment the line below and set REDIS_URL in your .env
# redis==5.0.1


# ── Optional: Async WebSocket Backend ────────────────────────────────────────
# Flask-SocketIO currently uses threading mode (async_mode='threading').
# To switch to eventlet for better async performance, uncomment below
# and add eventlet.monkey_patch() at the top of run.py.
# eventlet==0.33.3
